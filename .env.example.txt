# =============================================================================
# Watchtower Configuration Template
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git - it contains sensitive credentials
# =============================================================================

# -----------------------------------------------------------------------------
# Notion Configuration
# -----------------------------------------------------------------------------
# Get your integration token from: https://www.notion.so/my-integrations
NOTION_TOKEN=secret_your_integration_token_here

# Your main Movies & Shows database ID
# Find this in your database URL: notion.so/YOUR_WORKSPACE/THIS_IS_THE_DATABASE_ID
DATABASE_ID=your_database_id_here

# Optional: Releases/changelog database ID (for add_release.py / view_release_notes.py)
RELEASES_DATABASE_ID=your_releases_database_id_here

# -----------------------------------------------------------------------------
# TMDb API Configuration
# -----------------------------------------------------------------------------
# Get your API key from: https://www.themoviedb.org/settings/api
TMDB_API_KEY=your_tmdb_api_key_here

# Your region code for streaming availability (e.g., US, GB, CA, AU)
TMDB_REGION=US

# -----------------------------------------------------------------------------
# Email/SMS Configuration
# -----------------------------------------------------------------------------
# SMTP server settings (Gmail example shown)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465

# Your email address (for sending notifications)
EMAIL_FROM=your_email@gmail.com

# Email app password (NOT your regular Gmail password!)
# Generate at: https://myaccount.google.com/apppasswords
EMAIL_PASSWORD=your_app_password_here

# SMS recipients (email-to-SMS gateways)
# Format: phonenumber@carrier.gateway.com
# Common US carriers:
#   Verizon: @vtext.com
#   AT&T: @txt.att.net
#   T-Mobile: @tmomail.net
#   Sprint: @messaging.sprintpcs.com
SMS_RECIPIENTS=1234567890@vtext.com, 0987654321@vtext.com

# SMS delivery mode
# Options:
#   individual = BCC-style (each recipient only sees their own number)
#   group = All recipients visible to each other
SMS_SEND_MODE=group

# -----------------------------------------------------------------------------
# Digest Configuration
# -----------------------------------------------------------------------------
# Title prefix for daily digest pages in Notion
DIGEST_PAGE_TITLE=📩 DAILY DIGEST

# -----------------------------------------------------------------------------
# Notification Preferences
# -----------------------------------------------------------------------------
# Enable/disable specific notification types (true/false)

# Notify when any show has a new episode today (regardless of watch status)
NOTIFY_NEW_EPISODE_ANY_SHOW=false

# Notify when a show you're WATCHING has an episode airing today
NOTIFY_EPISODE_AIRING_TODAY=true

# Notify when a show you're watching has its season finale today
NOTIFY_SEASON_FINALE=true

# Notify when a full season drops at once (e.g., Netflix)
NOTIFY_BULK_SEASON_DROP=true

# Notify when a show you're watching returns within 7 days
NOTIFY_RETURNING_SOON=true

# Notify when a currently-airing show hasn't had an episode in 30+ days
NOTIFY_LONG_GAP=true

# Notify when 3+ of your shows have episodes airing tonight
NOTIFY_MULTIPLE_EPISODES_TONIGHT=true
